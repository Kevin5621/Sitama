class Logbook {
  final String id;
  final int title;
  final DateTime date;
  final String description;

  Logbook({
    required this.id,
    required this.title,
    required this.date,
    required this.description,
  });

  get weekNumber => null;
}